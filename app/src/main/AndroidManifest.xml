<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MOBAApp">
        <activity android:name=".LogoutActivity"></activity>
        <activity android:name=".homepage" />
        <activity android:name=".passwordresetActivity" />
        <activity android:name=".signupActivity" />
        <activity android:name=".GynecologyActivity" />
        <activity android:name=".DermatologyActivity" />
        <activity android:name=".CalendarandTime" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>